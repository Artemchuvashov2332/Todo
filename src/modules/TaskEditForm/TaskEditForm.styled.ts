import styled from '@emotion/styled';
import { Box } from '@mui/system';

export const StyledEditForm = styled.form({
  width: '100%',
});

export const StyledFormWrapperBox = styled(Box)({
  minHeight: '400px',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
});
